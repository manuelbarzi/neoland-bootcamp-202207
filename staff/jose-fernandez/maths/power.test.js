describe('power', function () {
     test('10, 4', function() {
         check(pow(10,4), 10000)
     })
     
     test('3, 4', function() {
         check(pow(3,4), 81)
     })
     
     test('2, 3', function() {
          check(pow(2,3), 8)
      })
   
   
   })
   
