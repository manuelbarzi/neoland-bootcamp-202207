function at(array,start) {
    //TODO
    // result=''
    if(start<0)
    start=array.length+start

    // if(start>array.length)
    // start=undefined


    // result=array[start]
 
    return array[start]
}

// console.log(at(['cat', 'dog', 'elephant',1,2,3], -2))
// expected output 'elephant'


// expected output 'Adiós'