function every(element, array) {
  for (let i = 0; i < array.length; i++) {
    if(array.length[i]<= element){
      return false
    }
        
  }
  return true
 }

// if(callback(arrayOfNumbers[i]))
// if (element <= arrayOfNumbers[i]);{
/*   if (array[i] = element) return (result = false);
    else result = true;
  }
  return result;*/

// si es un array [] vacio siempre devuelve ¡TRUE!
// else if(result === element);
